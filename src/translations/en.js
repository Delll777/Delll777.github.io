const NavBar = require('../data/nav-bar/en')
const Chat = require('../data/chat/en')
const PageMainMenu = require('../data/page-main-menu/en')
const StartTask = require('../data/start-task/en')
const Login = require('../data/login/en')
const GamePreparation = require('../data/game-preparation/en')
const GameResult = require('../data/game-result/en')
const Game = require('../data/game/en')

const en = {
  ...NavBar,
  ...Chat,
  ...PageMainMenu,
  ...StartTask,
  ...Login,
  ...GamePreparation,
  ...GameResult,
  ...Game,
}

export default en
